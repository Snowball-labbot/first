# V6 图文修订与复现

最新检查点：用户再次要求重做图4、5、7，已用MATLAB改成效率曲线、费用平面权衡和逐层改进轨迹；最终使用scripts/v6_focus_figures.m覆盖早期版本。本轮额度最后读取剩6%，提前同步。Word已完成局部替换并证明63表、49原生数学对象和模板不变；最终图片标注修正后的Word重建、完整渲染和最终检查仍在推进，以verification.json及后续发布记录为准。

本轮以 GitHub main 的 V5 最后提交6ea382b为底稿，最新模型与五份结果工作簿仍为artifacts/v5b。V6专门修改图像表达与正文，不改变模型、数据清洗、费用口径、参数选择和数值结果。用户要求保留模板、固定配色、单Agent工作；均沿用。

## 交付

- 完整论文：reports/完整论文_V6.docx；配套Markdown、LaTeX公式源及PDF。
- MATLAB源程序：scripts/v6_figures.m。
- 每幅新图：figures/v6中的360 dpi PNG、SVG、PDF与可编辑FIG；灰度检查版在_qa子目录。
- 图表数据与来源指纹：artifacts/v6/figure_data.mat、figure_sources.json。
- Word模板继承与修改记录：source_inventory.json、word_build.json、editorial_changes.json。
- 核验：artifacts/v6/verification.json，模型轨迹沿用V5b验证。

## 逐图调整

| 图号 | V6处理 | 数据与论证范围 |
| --- | --- | --- |
| 1 | 黑白直角框、水平分支与虚线递进 | 根据V5最新模型重写节点，包括连续DP、库存价值和调单机会 |
| 4 | 紧凑零基线三柱与节费括号 | 保留第一问主模型、无储能和效率解释对照 |
| 5 | 两面板离散点图 | 一月候选总费与紧急费；不把离散分位候选画成连续函数 |
| 7 | 费用分解加月度累计收益 | 原四种基础策略加V5最终方案；配对比较库存控制增益 |
| 9 | 验证费用矩阵加顺序更新对照 | 原24组验证数据，显示50%余量及全部更新的选择依据 |
| 10 | 更新组合矩阵加全年节费条形 | 原八种更新组合与同一风险余量；下方列出V5最终费用 |
| 11 | 全年时刻热力图加月度分位区间 | 附件4全部365×144点，月均值及10%—90%分位范围；无平滑或删点 |
| 13 | 直接标值MAE矩阵 | 同期/岭回归/GRU，四个发布时刻；低误差浅色，GRU行紫色框 |
| 15 | 预测器贡献、月度节费、配对区间四面板 | 上方保留基础执行器，下面使用V5库存价值增益，禁止混用比较基准 |
| 16 | 累计名义费用差距加剩余上限 | 共同名义可行域下1.002%的定义保持不变 |

图2、3、6、8、12、14的供需、调度和预测曲线保留，源图、表格与计算数值不变。全年实际价格的范围图被结构图替代，不复制Nature论文的地图、网络或样本。

## 参考与配色

已阅读用户提供的C050蔬菜补货论文、Modex论文的流程及正文页，并逐图查看用户提供的Nature参考图。只借鉴黑白层级、紧凑多面板、热力矩阵和区间标记等表达方法，不复制参考论文的模型结论、图形数据和原句。正文无需把这些视觉参考列为模型方法来源。

颜色严格取自docs/VISUAL_STYLE.md和插图配色/mgstyle.py：普通购电蓝、紧急费深红、库存/GRU紫、基线灰、电价暖色连续色标。黑白流程使用宋体，其余图为清晰的中文无衬线字体。保留矢量源及FIG，方便后续用MATLAB调整。

MATLAB R2026a Update 5在用户完成登录后成功验证许可并实际执行。第一次沙箱内启动报文件系统错误，正常启动后发现未授权；用户授权后已恢复。通用字体发现函数对中文字体名发出未找到首选字体提示，绘图显式指定Microsoft YaHei/SimSun并通过成图检查；日志不作为缺字已排除的唯一依据。自动几何检查和目视检查共同执行，不能仅因程序通过就声称图形没有遮挡。

## 正文与模板

改写重点是结果、机制和结论：明确库存转移为何能节费，补充V5最终方案的图文连接，删除重复自我限制用语。保留GRU费用区间跨零、历史路径价值属于近似以及名义最优界的适用条件，避免把措辞强化成未经计算的结论。

直接对V5 DOCX包中的10个图片文件和主文档XML进行局部替换。样式、节设置、页眉页脚、所有表格和全部原生数学对象保留，原V5文件指纹不变。图宽沿用原位尺寸，图高按新图比例计算；正文增删自然重排页码，不另换模板。

## 复现

先运行src/v6_prepare.py，默认从上一级CUMCM2026Problems/C题读取原始附件；再用MATLAB调用：

    addpath('仓库绝对路径/scripts');
    v6_figures('仓库绝对路径','本机math-modeling技能根目录');

随后运行：

    python -m src.v6_document
    python scripts/render_word.py reports/完整论文_V6.docx --out .qa/v6/render --dpi 110
    python -m src.v6_verify

Word使用工作区提供的Python依赖和现有Word原生导出。将渲染所得最终PDF保存为reports/完整论文_V6.pdf后再次运行验证，记录最新页数与指纹。修改任何图片后必须重新运行Word局部替换、渲染和验证，不能沿用旧PDF。

本轮不重复运行V5建模流水线，避免旧发布脚本覆盖新文稿。未跟踪的figures/q12_addon与scripts/plot_addon_q12.py属于其他工作，不纳入V6提交。
