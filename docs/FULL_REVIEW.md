# 四问完整初稿复查与交接

2026-09-11。当前交付 `reports/完整论文.docx`、对应 Markdown/LaTeX 公式，以及 `artifacts/q34/result3.xlsx`、`result4-2.xlsx`、`result4-3.xlsx`。前两问旧稿和结果保留。

## 已执行

- 附件3：365天、4个发布版本、24小时，共35,040个预测值。1,095个空日期为四行组的结构性留白，只补日期标签，不插补数值。小时端点用分段线性函数在十分钟区间积分；发布时刻锚点只取刚完成的实际区间。未来版本不能进入过去决策。
- 附件4：52,560个价格，按日期和右端点时刻逐项核验。价格范围0.0076—1.7936元/kWh。没有删峰、缩尾或平滑实际观测。原始指纹与清洗记录见 `artifacts/q34/input_audit.json`。
- 第三问：滚动混合整数规划；一月验证冻结12:00、18:00更新及零额外余量，2—12月334天总费用14,088,564.93元。八种更新时间完整比较，另有不退款口径敏感性实验。
- 第四问：同期、岭回归、3个随机种子的残差GRU及预先定义的平均集成。39次CPU训练约126.56秒；按月用过去数据重训，内部验证与训练标签不跨越切分边界。一月按实际策略费用冻结日前岭回归、滚动同期预测；对应14,880,535.05和14,790,501.42元。全年表现不能反过来选择主方案。
- GRU集成价格MAE 0.04366，岭回归0.04750元/kWh。误差评价共有120,240个发布时刻—目标区间配对，不是独立观测。更低预测误差不保证更低电费。
- 13项单元测试通过。21份全年轨迹已重建计划版本，检查物理约束、跨日SOC、原始观测一致性及完整费用账本；见 `verification.json`。
- 3份新增Excel共488,189个单元格与精确导出数据一致，无Excel错误单元格。抽查各类渲染预览；见 `delivery_verification.json`。
- Word：53页，摘要至参考文献25页，附录28页；30个编号公式、42个原生数学对象、16图、29表、10份完整计算程序。A4，页边距≥2.5cm，无目录，匿名属性，三线表。OOXML校验通过，53页渲染总览复查；代码附录有自然换行，原始可执行程序同时保存在仓库。

## Skills 与适用范围

使用用户指定的 `skills source/cumcm-c-problem-lfs/SKILL.md` 及其质量门禁、论文结构、图表规范；结合已安装 math-modeling、编程手、论文手、科研可视化工具、DOCX工具和 spreadsheets。图表沿用用户提供 mgstyle 配色，科学图用Python/Matplotlib真实数据生成，保存PNG/SVG及灰度检查图。没有使用 subagent，也没有声称独立多人评审。

C题技能内的LaTeX专用图表检查器不直接适用于Word；使用原生OMML、图表计数、文件哈希、完整代码逐行读回与Word渲染替代对应门禁。官方格式以 https://www.mcm.edu.cn/html_cn/node/4cd596519c9eb9fbd866398f6df0caa3.html 为参照：正文不超过30页，附录另计。技能建议页数不是额外比赛硬性规则。

## 必须保留的限制

主模型将充放电两侧效率分别解释为90%。第三、四问减购主假设为原价退款再扣50%违约费，逐次相对上一版计划结算；不退款且另罚50%已做固定策略敏感性比较，但没有为该口径重新调参。第四问按交付区间实际价结算，预测价只用于决策。以上需参赛队结合题面解释最终确认，不能隐去。

第二至四问为预测驱动的名义优化与实时平衡启发式，不声称随机动态控制全局最优；单个一月外部验证期、三种种子和一年数据亦不足以证明普遍泛化。最终初稿仍需队员通读，按竞赛平台要求填写AI使用声明和提交信息。本轮没有代为正式参赛提交。

## 复现

见 `docs/RUN_Q34.md`。验证入口 `src/verify_q34.py`、`src/verify_full_delivery.py`；完整稿生成顺序：`src.full_report` → `src.build_full_word` → `scripts/render_word.py` → `src.verify_full_delivery`。图表重建使用 `src.plot_q34 --data-root <原题目录>`。Excel重建需先运行 `src.export_q34_payload`，再运行 `scripts/export_q34_xlsx.mjs`。导出临时payload被忽略，可再生；Python训练环境 `.venv-q34` 不上传。

五小时额度到剩余5%时已触发保存和GitHub同步，不使用额度重置信用。后续账号先读本文件及HANDOFF；不要按照历史记录把三、四问当作尚未执行。
