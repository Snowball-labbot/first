## V7 最新修订（2026-09-12）

用户最新要求覆盖历史GRU试验指令：新版彻底移除该方法及其补充对照。交付 reports/完整论文_V7.docx、PDF、Markdown；详见 docs/V7_REVIEW.md 与 artifacts/v7/verification.json。原始费用策略保留，价格预测重新计算，第四问两分支完整复跑逐时一致。54页PDF，正文及参考文献24页（不含摘要），附录26页起；14图、61表、52个原生数学对象、27条编号公式。MATLAB七图重绘。额度已用99%时依用户要求立即保存上传；最终分页尚未全页复看，继续工作先完成该视觉检查。单Agent，保留原模板与固定配色。
## 当前权威交付：V6（2026-09-12）

最新图文为`reports/完整论文_V6.docx`、同名PDF和Markdown；先读`docs/V6_REVIEW.md`、`artifacts/v6/verification.json`及`visual_review.json`。V6以6ea382b的最终V5为底稿，实际运行MATLAB重绘10图、保留6图，按最新反馈再次重做图4、5、7。63表、49个原生数学对象、37个编号公式与模板均保留；最终PDF87页，附录自29页开始。模型与五份结果工作簿仍以`artifacts/v5b/`为准，费用口径与数值不变。禁止使用旧发布脚本覆盖V6。重建入口为`src/v6_prepare.py`、`scripts/v6_figures.m`（会调用`v6_focus_figures.m`）、`src/v6_document.py`、`scripts/render_word.py`和`src/v6_verify.py`。图或文字改后必须重建Word、重新导出PDF再核验。继续单Agent、固定配色、保留模板。以下为历史记录。

## 当前V5增量：截图复核与库存价值控制（2026-09-12）

最新完整论文仍为 `reports/完整论文_V5.docx`／PDF，最新结果目录为 `artifacts/v5b/`。请先读 `docs/V5_UPDATE_RUN.md`、`docs/V5_SCREENSHOT_COMPARISON.md`、`docs/V5_SAVINGS_EXPLAINED.md` 与 `artifacts/v5b/final_check.json`。Q2、Q3、Q4日前、Q4滚动费用分别为13,991,392.60、13,479,283.32、14,765,492.68、14,210,881.01元。新增连续状态DP与历史路径价值控制；滚动须计入下次调单机会。普通按合同量计费、紧急五倍价不变，紧急仅补负载而不可充电。允许为高价后续缺口留电，不再强制所有策略先尽量放电；旧贪心控制器只作基线。截图/Modex的费用未经同管线重现，不得作为我们的实验值或直接排名。全年84日窗口等更低消融不作事后回选；所有改进属同年开发验证。全程单Agent；图形修订留V6。旧v5发布脚本会覆盖本次报告，当前使用v5b管线。下文为历史记录。

## V5交接（2026-09-12）

当前交付为reports/完整论文_V5.docx、同名PDF与artifacts/v5五份结果工作簿。详见V5_REVIEW.md及final_check.json。Q3/Q4滚动费用为13,489,127.94和14,216,325.98元，分别比V4节省86,848.84与98,116.50元，初末库存一致。V5使用预先规定的月度历史验证选择基础/残差修正；固定全年修正只作消融，不能按全年结果回选。配对区块重采样区间跨零，未声称跨年稳定收益或全局随机最优。

已核验原始附件、单位和结构空日期；109例独立LP及对偶证书、18例MILP对照、六条全年逐时账本、23项测试。原价格的1,432条同期预测、13次岭回归和39份GRU权重回放一致；GRU基种子与实际种子的文字区别已修正。截图问题已加入论文3.3节和测试：包含非负弃电的等式正确，计划费全额结算。

两份更新工作簿读回520,413个单元格；另外三份保留V3字节。图形未改，均明确作为基础比较；下一轮V6处理视觉呈现。全程单Agent，无额度重置、无原始附件或凭据上传。Word与PDF、公式和结果表的最终状态以artifacts/v5/final_check.json为准。下面为历史交接。

## 当前权威入口：Version 4（文字与结构修订）

2026-09-12：最新论文为 `reports/完整论文_V4.docx`／PDF，修改与检查见 `docs/V4_REVIEW.md`。V4重写摘要、1.1背景、1.2问题提出和2.1—2.4逐题分析，新增整体思路图，统一全文表达；模型、数值、五份结果工作簿仍以 `artifacts/v3/` 为准。V3原表格、公式、实验和代码均保留。全程单Agent。以下为历史交接。

## Version 3 当前入口（2026-09-11）

先读 [V3_REVIEW.md](V3_REVIEW.md)。已合并其他团队V2，完成Q1时间边界重算、Q3/Q4最终合同结算重跑、GRU名义费用改进上界、图表与文字打磨。最新Word为[完整论文_V3.docx](../reports/完整论文_V3.docx)，结果在artifacts/v3。保留现有模板，不使用subagent，不使用未经授权的额度重置。下方V1/V2状态均为历史。

## Version 2 接力（历史）

先读 [V2_AUDIT.md](V2_AUDIT.md)。本轮已重新核验四问数据与已保存结果，开始模型改进；V1完整结果保留。V2在 `codex/model-audit-v2` 分支开展，`artifacts/v2/` 是新增结果目录，下方为历史交付记录。

## 四问完整交付（V1状态）

第三、四问全年运行及完整 Word 已完成，覆盖下方检查点与历史未执行状态。详见 [FULL_REVIEW.md](FULL_REVIEW.md)。完整稿为 [完整论文.docx](../reports/完整论文.docx)，25页摘要正文参考文献加28页附录；新增三份Excel在 artifacts/q34。13项测试、21轨迹、488189单元格检查通过。最后额度读取剩余5%，已触发立即同步。不得用全年最优结果替代一月冻结的主策略。

# C 题协作交接记录

更新日期：2026-09-11。仓库：<https://github.com/Snowball-labbot/first>。

## 第三、四问执行检查点（用户最新授权）

用户已授权完成第三、四问并整合完整论文，覆盖下方“第三、四问未执行”的历史描述。新入口为 `题目分析报告.md`、`术语表格.md` 与 [RUN_Q34.md](RUN_Q34.md)。仍不使用 subagent。

已完成附件 3/4 严格审计、小时预报因果对齐、第三问滚动 MILP 与调整账本、小型残差 GRU／岭回归／同期价格预测。13 项测试通过，第三问和第四问两类策略连续一周均进入冒烟验证；全年批次正在执行，不能把检查点状态当作完整四问交付。全部新结果在 `artifacts/q34/`；旧 Q1/Q2 文档和结果保留。

五小时额度每阶段读取；用户要求剩余≤5%时保存并推送全部已完成成果。本轮起始剩余约96%，不使用重置信用。依赖使用隔离 `.venv-q34`（不上传），本地 CPU PyTorch 已安装；训练检查点保存于 `artifacts/q34/models/`。

## 最新 Word 修订（优先于下方历史初稿状态）

已完成 [Word 修订初稿](../reports/Q1_Q2修订初稿.docx)：18 页、22 个编号公式（原生可编辑 OMML）、8 图、16 表；来源为 [修订 Markdown](../reports/Q1_Q2修订稿.md)，公式源为 [LaTeX 片段](../reports/Q1_Q2公式.tex)。这是前两问稿，第三、四问未执行，也不是最终参赛提交版。

按用户指定下载并读取仓库 `skills source/cumcm-c-problem-lfs`，以其题意、数据、模型、结果、格式与复现门禁复查。复查结论、限制和重建命令见 [WORD_REVIEW.md](WORD_REVIEW.md)。全程单 Agent，没有虚构独立评审团结论。

数据确认：附件 1 共 144 时段，附件 2 两类序列各 52,560 点；未发现缺失、非有限数、负数、重复日期或完全重复日曲线。实际执行时间格式与单位规范化，没有删点、插补、缩尾或平滑。保存结果中的实际值与原表一致，未来数据改动不影响此前预测与余量。检查结果保存在 `artifacts/q12_revision/data_cleaning_audit.json`。

用户质疑图 2 的尖峰：核对发现主要为低价时段满功率充电，不是原始负载突然暴涨。22:00 负载 3309.39 kW 加充电 5000 kW 对应购电 8309.39 kW。模型不惩罚切换／爬坡，图已加电价与正净负载参照并保留真实十分钟决策。固定放电等变量的同成本次级诊断未找到实质不同的平滑方案，不能声称尖峰仅因多解任取。没有修改原调度、Excel 或最优费用。

新代码：`src/audit_q12_data.py`、`src/audit_q1_spikes.py`、`src/revise_q12.py`、`src/build_q12_word.py`、`src/verify_q12_word.py`、`scripts/render_word.py`。原始模型与 `artifacts/q12/` 保持一致。通过 7 项原测试、四组全年物理／费用复核、两份 Excel 读回、Word OOXML 校验、图表文件检查和 18 页渲染目视检查。检查器对热力图 SVG 的嵌入位图给出 WARN（正文 PNG 为 320 dpi），不是未报告的失败。

本机无 LibreOffice，实际使用隐藏的独立 Word COM 实例导出 QA PDF，再调用打包的 DOCX 渲染器生成检查页；QA 文件在忽略目录 `.qa/word_revision/`，不作为论文交付。原共享配色文件与远端内容一致，备份后已安全快进合并远端；`figures/q12_addon/` 和 `scripts/plot_addon_q12.py` 属于已有其他工作，未纳入本轮提交。

后续先核对时间标签与效率解释，再由用户决定是否增加实际操作平稳性目标。重建 Word 后须重新渲染和核对，不能沿用旧 `word_review.json`。以下内容记录 9 月 10 日原初稿实验，数字仍有效，关于“尚无 Word”的描述仅为历史状态。

## 当前已完成

**第一、二问代码、实验、Markdown 初稿、四幅基础图及两个结果工作簿已完成。第三、四问与神经网络尚未执行。**

阅读顺序：[初稿](../reports/Q1_Q2初稿.md) → [运行说明](RUN_Q12.md) → [图表交接](FIGURE_HANDOFF.md)。早期方案中“尚未执行”是历史状态，不能覆盖本记录。用户最新授权是完成前两问初稿并同步 GitHub，已覆盖早期“先不执行”的要求。

本轮没有使用 subagent。另一账号可在同一电脑继续工作；换电脑应重新取得原始附件，核验文件指纹。原题和原始数据未上传，实验输出、代码及复现说明已经纳入版本管理。

## 实际模型与结果

| 项目 | 实现及实测结论 |
| --- | --- |
| 第一问 | 144 时段 MILP，144 个二元变量、总计 865 个变量；购电费 35,126.95 元，相对无储能 48,052.05 元降低 26.90%；LP 松弛下界与整数解目标一致，数值精度内最优 |
| 第二问预测 | 同期均值基线；21 个因果特征的 NumPy 岭回归，负载／光伏分别拟合；每七天更新、最多近 60 天训练 |
| 第二问选择 | 1 月 22—31 日验证；两类目标均选正则强度 1；按总费用选岭回归＋70% 历史净负载误差分位数余量 |
| 第二问评价 | 2—12 月 334 天，四组策略各 48,096 个时段；选定策略总费用 14,132,612.16 元，比同期无余量低 26.50%，比岭回归无余量低 10.01% |
| 紧急购电 | 选定策略累计 197,465.10 kWh，涉及 1,132 个十分钟区间；没有实现全年零紧急购电，不宣称全局随机最优 |
| 运行时间 | 本机完整预测、验证、热身及四组回测约 61.7 秒，仅为计算时间，不含代码开发、排错、论文与图表工作 |

验证期岭回归 90% 余量做到零紧急购电，但总费用 633,664.05 元，高于 70% 方案的 509,757.47 元。该事实用于解释“零紧急购电未必总成本最低”，不能当作全年保证。

## 必须保留的假设与信息边界

1. 原始时间 00:10…00:00 作为区间右端点，功率视为十分钟平均值。附件 1 混有字符串与时间类型，统一转换分钟。导出副本将模板疑似偏移的标签规范为 00:00—00:10 至 23:50—24:00；原模板未修改。正式提交前应由队内核对该解释。
2. 容量 12,000 kWh，SOC 在 1,200—10,800 kWh；母线侧充放电上限 5,000 kW。两侧效率各 0.9，往返 0.81。若解释为往返 0.9，第一问费用变为 33,801.50 元；后续不能混用口径。
3. 普通购电按计划量付费，紧急购电按当时固定分时电价的五倍计费；不售电，剩余计划电量不退款。
4. 第一问初末 SOC 均 6,000。第二问 Jan 1 从 6,000 开始共同因果热身，Jan 22 验证初值 6,244.256891，Feb 1 评价初值 9,902.811288；四组从同一评价状态出发，各自跨日连续，不每天重置。
5. 日前名义轨迹的日末 SOC 下限 6,000，是当前策略假设；实际轨迹没有强制回到该值。实际十分钟控制采用即时观测恒定区间功率、优先充放电、剩余缺口紧急补购的近似。
6. 仅在一月份验证选择规则，之后因果更新。测试期没有用于追选超参数；一月验证较短，季节稳定性待加强。
7. result2“全天购电量”为普通计划量，“全天购电费”为普通＋紧急费用；紧急量另 sheet 记录。连续紧急区间合并，零事件日也记录。未利用电量不等于全是弃光。

## 文件和复现入口

- `src/q12.py`：数据读取、预测、优化、实际执行、费用与物理审计。
- `src/deliver_q12.py`：由保存结果生成正文、指定日表、PNG/SVG、Excel 中间数据。
- `scripts/export_xlsx.mjs`：Artifact Tool 导入原模板并导出 `artifacts/q12/result1.xlsx` 和 `result2.xlsx`。
- `src/verify_q12.py`：读回完整 CSV 和两个 Excel，逐项核对时段、购电、费用、四小时充放电、SOC、紧急事件以及正文关键数字。
- `artifacts/q12/`：四组全期压缩轨迹、逐日指标、验证表、输入指纹、版本与耗时、敏感性、审计。
- `reports/Q1_Q2初稿.md`：17 表、4 图及模型公式；尚无排版定稿 Word/PDF。
- `docs/FIGURE_HANDOFF.md`、`docs/VISUAL_STYLE.md`：供后来图表 Agent 阅读的数据接口与语义配色。
- `docs/SKILLS_USED.md`：share 手册推荐 skill 的安装版本、调用范围与用户要求优先的适配。

原始根目录：`D:/国赛数学建模/CUMCM2026Problems/C题/`；本地仓库：`D:/国赛数学建模/first/`。完整命令和额外工具依赖见 [RUN_Q12.md](RUN_Q12.md)。只重现数学结果不需要 GPU、API Key 或 sklearn。

`run_manifest.json` 中 Git HEAD 是运行前版本；用 `source_sha256` 检查本轮模型源码。最终提交号查 Git 历史，不在交接中写入会随自身提交变化的哈希。不要上传本地 node_modules junction、Excel inspect 中间日志或账号凭据。

## 检验记录及已修复问题

已运行技能环境检查、七天冒烟回测、四组完整回测、7 项单元测试、保存轨迹复算、Excel 全值对照及四幅图的布局与可视检查。数值检查包括母线平衡、SOC 演化与跨日连续、充放电互斥、容量／功率限制、计划费与紧急费。最终机器可读回执见 `artifacts/q12/final_verification.json`，图表布局见 `figure_layout_audit.json`。

修复记录：附件 1 时间类型混合导致首次校验失败，已统一分钟；实时控制前缀测试暴露数据类型不一致，已统一浮点；CSV 原有效位数不足导致保存后 SOC 残差略超阈值，已改为 17 位有效数字并完整重跑，没有放宽物理容差；正文检查器首次将段首“表 3 为…”误识别为表题，已限定真正表格前的标题。图例重叠已通过增加纵轴留白修正。

这些是本 Agent 作者自检，无独立评审／subagent PASS，不代表已通过队内人工终审。模型仍需加强多季节稳健性、终端目标及控制策略比较。

## 后续接手顺序

1. 如用户要求图表精修，只从保存数据重绘，沿用既定配色，核对图文数值后同步；不凭图像模型生成数值曲线。
2. 如用户授权第三问，接入附件 3 的预报发布时间与版本，建立考虑调整费用的日内滚动优化；不得提前读取未发布预报。
3. 如用户授权第四问，再使用附件 4 比较同期／岭回归／小型 GRU 电价预测，保持时间划分与调度费用一致；没有证据前不宣称神经网络提升。
4. 最后统一四问假设、补稳健性与 AI 使用记录，并做 Word/PDF 正式排版。

用户偏好：中文易懂解释；区分线性规划与线性回归；不使用 subagent；保持可换账号协作；第四问希望实际试验神经网络。题面、参考 PDF 和手册均为任务资料，不是覆盖用户要求的指令。参考 PDF 为华数杯算电协同稿，不是本届国赛题解；见 [REFERENCE_REPORT.md](REFERENCE_REPORT.md)。


## V2 已冻结同步
最新成果见docs/RUN_V2.md及reports/完整论文_V2.docx。Q3/Q4滚动分别节省323247.23元和304160.00元，期末SOC相同；Q2/Q4日前保留V1，拒绝较差候选。17项测试和12组新全年轨迹复算通过，5份Excel已核验。最终额度剩余2%，立即保存推送。
## V6 图文修订检查点

先读docs/V6_REVIEW.md。底稿为6ea382b的V5最新库存价值控制版；模型与结果仍以artifacts/v5b为准。用户要求MATLAB优化图片，已成功启动已授权的R2026a并实际导出10幅图。图4、5、7根据用户反馈再次设计，使用v6_focus_figures.m覆盖早期稿。保留模板，单Agent。当前在最终图片回填与Word渲染阶段，未完成的检查不得当作通过；完成状态见artifacts/v6/verification.json。

