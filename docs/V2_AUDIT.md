# Version 2 第一阶段核验

核验基线：`7c8be16baa139a9ee9ad471bb742cf27463a9d46`，日期 2026-09-11。原始附件从命令参数 `--data-root` 读取；旧模型、论文与结果保留。本轮按用户要求先核验并同步，再开展优化。

## 已实际完成的复算

- 通读三页题面、四问完整 Markdown、模型方案、数据合同、实验方案，以及 Q1/Q2、Q3/Q4、价格预测、导出与检查代码。
- `python -X utf8 -m src.verify_q12 --data-root <数据根目录>`：退出码 0。4 组全年轨迹，供需守恒、SOC、计划费和五倍紧急费，两个工作簿及原稿主数值通过。
- `python -X utf8 -m src.verify_q34 --data-root <数据根目录>`：退出码 0。21 组全年轨迹、逐版本调整账本、原始负载/光伏/电价一致性、费用重算及 39 次拟合截止时刻通过。
- 数据尺度：附件1为144时段；附件2负载/光伏各52,560点；附件3为35,040条小时预报值；附件4为52,560点。原表不删点、不填造数值、不平滑。
- 上述是重新运行核验程序的作者复查，不等于第三方独立评审；完整 Word 的新版渲染与重新导出尚未开展。

## 问题与优化优先级

| 编号 | 发现 | 证据/影响 | V2处理 |
|---|---|---|---|
| A01 | 多处交接文件仍写第三、四问未完成 | `AGENTS.md`、`HANDOFF.md`、`DATA_CONTRACT.md` 的历史状态与最新稿冲突 | 增加统一版本入口，标明旧记录的适用版本 |
| A02 | 第四问指定日输出展示不完整 | 完整稿附录A.3仅8行日汇总；缺两类策略的6个指定时段、6个储能区块及逐事件紧急购电表 | 从真实轨迹自动生成全部明细 |
| A03 | 日内负载预测未吸收当日已发生偏差 | `q34.backtest` 每个发布点使用午夜的 `pred['load'][day,t0:]` | 检验只用已完成区间的负载偏差校正，并与相同调度规则做消融 |
| A04 | 混合整数模型存在可利用的结构 | 允许非负无上界弃电；互斥通过保持SOC和购电不变、消除同时充放电实现 | 给出证明；实现LP并以MILP和手算实例交叉核验，不预设耗时提升 |
| A05 | 风险余量与终端下限仅以一月短窗选择 | Q2固定70%、Q3固定0及6000kWh终端下限，全年可能存在季节不适配 | 在相同信息条件下试验预先固定的因果自适应规则，保留未改善的结果 |
| A06 | Q4价格误差汇总混合4个发布起点 | 120,240个起点—目标配对，非独立观测；Q4-2只用午夜 | 补按发布起点与午夜单独的MAE/RMSE及决策费用差异 |
| A07 | 第三问退款、第四问结算时点、时间标签与效率有解释空间 | 旧稿已注明，不是已证实的计算错误 | 保留假设并补敏感性，不能把某一解释当作唯一官方规则 |

目前核验没有证明原主费用数字错误。模型可改进与数据算错是两种不同结论；新稿必须分开呈现。

## 预先约定的实验纪律

1. 新候选参数只用1月22—31日选择；全年指标不反向挑选最终参数。若使用逐月自适应，每次只能读取当时以前的完整日期。
2. 所有组使用共同初始SOC并各自连续运行；报告终端存量和月度费用，不能通过重置电池创造节省。
3. 不宣称新一轮在已读过旧全年结果的数据上构成全新的外部验证；这是同一数据集上的模型修订和顺序回测，需要未来独立数据验证。
4. 新结果写 `artifacts/v2/`，旧结果保持可追溯。所有数字从计算文件生成。
5. 本轮图表精修按用户要求后置；新诊断图避免柱状图。不使用subagent，遵循仓库约定。

## 技能与环境

已安装项目推荐的 `XiaoMaColtAI/math-modeling-skill` 固定版本 `3527fad922660397834a6167fc2b4c29ee64ba17`；读取本地角色和PDF/Excel入口。另读取远程仓库随附的 `skills source/cumcm-c-problem-lfs/SKILL.md` 和 `references/quality-gates.md`，用于题意、数据、模型、结果、验证检查。正式图表和排版数量建议服从用户本轮重模型、轻配图的范围要求。

隔离环境 `.venv-v2` 使用系统已有 NumPy/pandas，并安装 SciPy 1.17.1 与CPU PyTorch；不纳入Git。文件中不得保存凭据。用户已授权将本轮成果推送本仓库，不强推共享分支。
