# V9 图表与排版交付

本版以 main 的 f4cdab0（V8）为唯一正文与数据基准；从网站找到 f8d78c1 的新版V7，核对图3、6、8的布局与原色。没有使用早先已撤销的V7。

## 完成内容

14幅图统一重绘，交付文字SVG、Word兼容轮廓SVG、PDF、600 dpi PNG和灰度预览。图3恢复购电/供需/充放电、库存、电价三层结构；图6恢复四日四行两列；图8恢复较高的单轴版面。三图按用户追加要求保留V7的原色，其余图执行V9语义配色。第一问仍使用V8修正时段后的数据，绝不复用V7旧轨迹。

框架图按共享物理系统、信息递进、预测、决策和实时执行组织；参数与费用图采用分面、点线图、连续色矩阵和区间图。正文明确要求零起点条形的图4保留此图型。未增加虚构误差带，价格区间沿用已有历史残差与已有重采样结果。月度价格分位数沿用MATLAB的Hazen计算口径。

Word采用14个原生SVG扩展并保留高清兼容回退。线图、文字与公式在PDF中保持矢量；全年52,560格价格热图为内嵌高密度栅格，以控制文件体积，坐标和标注仍为矢量。其余费用矩阵为矢量单元格。

61张表统一三线、线宽、表头重复和单元格留白。保留A4单栏、原页边距、摘要模板和原生公式。48页中摘要1页、正文与参考文献23页，附录A从25页、代码附录从34页开始。全部48页已渲染逐页查看；大图按论证顺序自然分页，没有为压缩页数缩小图6。章节或附录末页允许自然留白。

## 内容边界与核验

唯一获准的文字改动：图6布局说明改为“图6按日期分行、按负载与光伏分列，同一列共用纵轴尺度。”其余1287个正文段落（含附录代码）、61张表格内容和52个原生数学对象与V8逐项相同，14幅图读取同一V8数据文件。摘要文字和版式保持；原生Word重新导出的抗锯齿存在0.084%的像素差异，不宣称逐像素一致。公式Tex文件直接复制，模型及结果工作簿继续使用V8，不另造重复结果。

内容校验见 `artifacts/v9/verification.json`；原始图表清单见 `source_inventory.json`；图源哈希见 `figure_manifest.json`；逐页图像对象清单见 `page_audit.json`。Office OOXML schema校验通过。此轮没有重新拟合、重算或宣称改进模型最优性，任务仅为视觉与排版。

复现顺序（在仓库根目录，使用带 scipy、matplotlib、python-docx、Pillow、PyMuPDF、pywin32 的Python）：

```powershell
python src/v9_inventory.py
python src/v9_figures.py
python src/v9_document.py
python scripts/v9_render.py
python -m src.v9_verify
```

PDF导出需要Windows Microsoft Word。V7、V8及其生成代码均原样保留；后续工作从V9实际文件开始，不使用旧生成器覆盖本版。
