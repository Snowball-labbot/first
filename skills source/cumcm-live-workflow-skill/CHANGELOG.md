# 更新日志（Changelog）

## v5.1（2026-08-25）· 实战排版细节修订（2024A 真题打磨沉淀）

### 编号与题注（评阅实测修订）
- **列表编号改阿拉伯**：`enumerate` 用 `label=(\arabic*)` (1)(2)(3)，经咨询相关人员确认与公式编号 (n) 不会混淆；正式取代原罗马 (i)(ii) 方案（SKILL §3 唯一权威同步为 v8）
- **图表题注编号后不加冒号**：`\captionsetup{labelsep=space}` → "图1 标题/表2 标题"；题注正文也用括号式"（0、60、120 s，单位：m）"替代冒号
- **公式编号完整性**：正文每个展示公式都编号（含证明中间盘算），禁止 `\[...\]` 裸公式；编译后程序提取 `(n)` 校验 1..N 连续无缺号

### 表格排版
- **全篇表格统一 `tabularx{\textwidth}` 撑满版心**，数据列居中 X，左右对齐正文、无大留白
- **禁用 `\resizebox` 压缩表**（字号与其他表不一致）；太宽的表按时刻/列组拆分分表（6列拆 3+3、5列拆 3+2），标题"表X（一）/（二）"，字号统一 footnotesize
- 程序化表格体检：booktabs 横线 bbox 对齐版心、单元格字号一致（PyMuPDF）

### 验证与来源表述
- **验证板块数据"文内联动"**：指向正文前文图表/式（"与表\ref/式\eqref 一致/互证"），不写"由附录脚本算得/存于支撑材料"式原始出处

### LaTeX 附录与字体
- **lstlisting 换 Consolas**：lmtt 缺希腊/数学符号字形渲染空白；XeLaTeX 下 listings `literate` 对 UTF-8 失效，改用 `\newfontfamily\listingmono{Consolas}`（代码注释里的 ∈ 改 ASCII）
- 附录小节标题过长溢出时括号前加 `\\` 换行

## v5.0（2026-08-25）· 一次发布（含全部历史迭代）

本版将 v1-v4 的历史迭代与今日全部增量统一为一次版本发布，主页只展示最终形态。

### 启动与环境
- **启动关键词**：流程/环境/终审三类触发词（国赛、完整做真题、环境检查、按自查表审查等），命中即启动
- **启用后自动环境检测**：输出前置条件完成度清单（Python/依赖库/LaTeX/ctex/poppler 逐项 ✓/✗）
- **确认后自动安装**：用户确认后自动补齐缺失组件（pip 清华镜像 / winget LaTeX），装后复测到就绪
- 红线：未获确认不安装任何软件

### 质量与规范
- 顶刊级图表规范：图表文字强制中文（SimHei）、全文统一配色（蓝橙绿 / Nature / 美赛 O 奖）
- 五段式摘要：背景段 + 逐问"针对问题X"独立段 + 收尾段，≤1000 字符、量化数字≥8
- 论文成品结构模板：完整 9 章骨架，每问固定"模型建立/模型求解/结果与分析"三小节；编号规则唯一权威（章节/列表罗马/公式三档）
- 文献与公式扩充标准：参考文献 15-25 篇（真实、近5年≥60%、中英文混合）、编号公式≥15
- 降重四层法（词汇<句法<逻辑<证据）与九类 AI 痕迹自查、把 AI 当审稿人
- 国奖级硬性指标清单：三类检验缺一最高省二、正文 25-30 页、表格≥18 张
- 2026 AI 合规：AI 工具使用声明、AI 详情 PDF、论文/支撑材料/附录三处对齐、26 条自查表终审

### 配套文件
- **references/ 可执行验证模板**：`verify_xlsx_rows.py`（模板行映射校验）、`verify_pdf_metrics.py`（PDF 结构/越界/匿名/关键数字体检）、`sensitivity_framework.py`（敏感性跑批）
- **WORKED_EXAMPLE.md**：水箱注水最小工作示例（从题面到可验证结果的完整链路）
- **templates/ 参考材料**：格式规范.pdf、AI工具使用规定(2026试行).pdf、AI自查表.docx、标准论文Word模板.doc、国奖级论文AI优化升级提示词.docx

### 附录与支撑材料规范（强化）
- 附录结构模板：支撑材料文件列表（三线表）+ 每个源程序独立附录小节，`\lstinputlisting` 全文
- 代码完整性铁律：附录脚本必须可直接运行复现论文结果，禁止省略/截断/伪代码/用"见代码文件"代替
- 文件列表一致性铁律：附录列表与压缩包逐项对齐（数量、名称一致）
- 提交前一致性检查：逐文件比对、抽查 2-3 个脚本实际运行复现、覆盖全部建模脚本（含图表/检验/敏感性）

### 历史里程碑（已并入本版）
- v1 全流程框架 → v2 质量与规范 → v3 AI合规与终审 → v4 开源仓库化（详见 git 历史）
