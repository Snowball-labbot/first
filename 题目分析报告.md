# 第一二问执行模型与假设

本轮只处理第一、二问，依据为用户提供的国赛 C 题及附件 1、2、5。完整研究路线见 `docs/MODELING_PLAN.md`；本文件固定本次实际实现的合同。

## 共同口径

每行功率按十分钟区间平均值解释，输入标签为右端点，电量＝功率/6。混合时间类型先转为分钟数，验证时序为 10—1440。输入文件不修改；结果副本将模板疑似错位的标签统一为 00:00—00:10 至 23:50—24:00，并附映射文件。

电池两侧效率各 0.9，母线侧充放电功率上限 5000 kW，内部储电 1200—10800 kWh。购电、充放电和未利用电量均为非负；二元状态保证充放电互斥，无售电收入。能量平衡与 SOC 方程见正文和 `src/q12.py`。

## 第一问

以附件 1 的预测光伏和负载为确定性输入，最小化 Σpg。日初与日末储电均为 6000 kWh。使用 HiGHS MILP 求解，再计算 LP 下界作为数值最优性证据。比较无储能基线，并计算另一种效率解释下的结果。

## 第二问

日前预测器为同期基线和岭回归。岭回归正则项只惩罚系数、不惩罚截距；21 个特征全部来自历史或已知目标日历。每七日更新、最多使用最近 60 天；正则强度候选 1/10/100。1 月 22—31 日验证，2—12 月顺序评价。

风险余量使用过去最多 28 天的净负载误差，池化目标槽前后三槽，取非负经验分位数。比较无余量和 70%/80%/90%，按一月验证总费用选择，不能根据全年结果重新调参。

计划优化将预测负载加余量后送入同一 MILP，预测轨迹日末 SOC 至少 6000 kWh。实际运行采用因果贪心平衡：当区间有盈余时尽量充电，有缺口时尽量放电，最后紧急购电。假设区间内功率恒定且可即时响应，不利用未来实际路径。

实际费用为全部计划购电费加五倍价格的紧急购电费。实际 SOC 跨日连续；所有评价策略在二月初采用相同的一月热身终态。本文是“预测＋风险余量＋名义优化＋实时运行”近似策略，不宣称为全年随机最优控制。

## 验证与交付边界

验证能量平衡、SOC 更新、跨日连续、非负、上下限、互斥、因果特征、因果运行前缀、费用复算与文件覆盖范围。CSV 保留 17 位有效数字，避免导出舍入掩盖或制造残差。结果副本的“全天购电量”填普通计划量，“全天购电费”填计划费与紧急费合计，紧急量另列。

按用户要求由单 Agent 执行，本轮是可检查的初稿与基础图，不是完整四问论文或独立评审已通过的最终稿。
