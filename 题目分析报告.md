## 当前权威入口：V7（2026-09-12）

最终论文为 `reports/完整论文_V7.docx` / PDF，核验见 `docs/V7_REVIEW.md`、`artifacts/v7/final_check.json`，复现见 `docs/RUN_V7.md`。第一问统一为区间终点均值，不旋转输入，费用35,126.95元；模板起点标签仅做周期输出映射。Q2/Q3/Q4日前/Q4滚动分别为13,991,392.60、13,479,283.32、14,765,492.68、14,210,881.01元，沿用V5b策略并重核数据与账本。普通按合同量收费，紧急五倍价且不用于充电，滚动以午夜与最终合同净差结算。正文重写为统一库存价值框架；截图比较留外部核验说明；结尾合并为模型评价与推广，含运行灵活性与容量配置。随机策略不宣称全局最优。全程单Agent。以下旧版本段落仅为历史，不得覆盖本段。

# 四问扩展模型合同（2026-09-11）

用户已授权执行第三、四问及整合论文，全程单 Agent。第一、二问冻结结果保留，新的模型和输出放在 `src/q34*`、`artifacts/q34/`。

第三问：在午夜及可选的 06/12/18 时刻读取当时发布的小时光伏预报，将其分段线性曲线积分为十分钟电量。发布时间处没有零小时预报，用最近已经结束的实际光伏区间均值作为边界代理；只计划到本日末，次日仍重新制定计划。该插值是预测分辨率转换，不是清洗／平滑实际观测。负载预测沿用第二问的因果岭回归。

主账本假设：取消已承诺电量后，按交付时段电价退回 50% 的金额（等价于原价退款并扣除 50% 违约费）；增购支付 1.5 倍。多次更新以最近版本为基准，每次增购按 1.5 倍、每次减购仅返还 0.5 倍，不能返还此前支付的 1.5 倍。因此增加后再取消的往返操作成本为 1 倍电价，不产生调整套利。公式为初始计划费用加历次 `1.5p×增量−0.5p×减量`，再加 `5p×紧急量`。另一解释“原款不退，取消另付 0.5p”作为重新优化的敏感性场景。两者均是假设，题面未充分说明。

每次滚动决策优化尚未执行后缀，锁定历史，保存全部计划版本与逐时结算明细。SOC 从当前真实已知状态开始并跨日连续；名义日末下限沿用 6000 kWh。充放电保持互斥，采用 MILP。历史误差余量只取此前完整日期，并匹配预报发布时间，不用当天尚未发生的真值。

第三问在一月验证误差余量和更新时刻选择，2—12 月 334 天评价八个更新子集。主结果只能由一月验证选定，不能根据全年最好组倒选。额外报告冻结午夜预报对照、日内信息价值及费用口径敏感性。

第四问：附件 4 给出实际交付时段电价，不视为日前已知。用历史价格预测未来 144 点，按 00/06/12/18 相同起点比较同期、岭回归和一层小 GRU。使用同一输入信息截止、日历特征、目标跨度和训练／验证时间，所有完整目标窗口必须在拟合截止前结束。主比较保留随机种子 17、42、2026，报告各次及平均表现，不选取最有利随机种子冒充稳定效果。

电价预测用于优化；实现费用用交付区间真实价格结算，这是单一价格序列能够支持的明确近似。两类调度分别回测并生成 result4-2、result4-3。模型更新只使用已发生数据；预先规定更新频率。没有气象数据、报价面板时不虚构输入。

统一评价：价格／功率 MAE、RMSE，总费用、普通／调整／紧急分解、SOC 初末存量、未利用电量、物理约束残差、预测生成时点与模型拟合截止。缺失、重复、负值、异常峰及合并单元格独立审计；不根据全期统计处理训练特征，不按图形是否好看修改数据。

工程门禁：真实数据单周试验与费用／因果／物理边界测试通过后再全期运行；逐阶段保存 GitHub 检查点。五小时剩余额度≤5% 时优先保存、交接和推送，不能把未完成实验记作通过。完整 Word 继续使用已下载 C 题与 DOCX skills；公式、图表和结果须互相核对。
