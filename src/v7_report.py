"""Reorganize the V6 manuscript around a single dispatch model family."""
from pathlib import Path
import re, json

ROOT=Path(__file__).resolve().parents[1]


def main():
    md=(ROOT/'reports/完整论文_V6.md').read_text(encoding='utf-8')
    def replace_section(start,end,new):
        nonlocal md
        i=md.index(start);j=md.index(end,i);md=md[:i]+new.strip()+'\n\n'+md[j:]
    def paragraph(prefix,new):
        nonlocal md
        matches=[p for p in md.split('\n\n') if p.startswith(prefix)]
        assert len(matches)==1,(prefix,len(matches))
        md=md.replace(matches[0],new,1)
    md=md.replace('# 基于风险校准与库存价值控制的微网购电调度','# 考虑供需与电价不确定性的微网购电调度')
    replace_section('## 摘 要','## 一 问题重述',r'''## 摘 要

针对微网计划购电、储能运行与日内调单相互耦合的问题，本文构建以实际购电费用为评价目标的统一调度模型。在共同的能量平衡和储能约束下，依次引入供需预测误差、预报更新和电价不确定性，以风险校准确定计划规模，以库存价值控制配置实时电量。

针对问题一，以购电与充放电量为决策变量建立多期线性规划。在允许自由弃电的条件下，利用保持储能状态和购电量的变量变换处理充放电互斥。最优日购电量为59,482.70 kWh，购电费为35,126.85元，较无储能降低26.90%，形成后续各问的物理模型基础。

针对问题二，在问题一中接入岭回归供需预测，以历史误差的70%分位数设置购电余量；结合过去28日的配对残差估计库存后续价值，在当前补购费用与未来缺口费用间选择放电量。2—12月334天总费用为13,991,392.60元，较相同计划规则下的即时平衡执行节省141,219.56元。

针对问题三，在问题二中增加0、6、12、18点的预报更新及合同调整权限，按保留、取消、新增量建立非对称结算的滚动优化模型。负载修正按历史费用逐月选择，库存后续价值计入下次调单机会。全年总费用为13,479,283.32元，表明日内信息需要与调整成本和储能状态共同考虑。

针对问题四，以历史价格预测替代原固定电价，分别扩展日前与滚动模型；未来价格用于预期费用计算，当前实际价格用于执行与结算。波动电价下两类策略的总费用分别为14,765,492.68元和14,210,881.01元，滚动方案费用低554,611.67元。历史残差范围用于展示价格预测的不确定性，供需风险校准与库存价值控制继续沿用前述结构。

结果表明，计划层控制供需偏差，滚动层利用新信息调整合同，执行层根据后续缺口费用配置有限库存，三者共同决定微网的运行经济性。

关键词：微网购电；线性规划；风险校准；库存价值；滚动优化''')
    paragraph('实际运行还受到','实际运行受到信息与交易规则的共同约束。运营者需在每天零点申报全天计划，实际负载与光伏尚未完全获知；计划不足会触发高价紧急购电，计划过量则形成额外支出。日内预报允许修订供需判断，但修改合同同样产生费用。波动电价进一步改变储能在不同时段的价值，使购电、调单与放电时机需要联合确定。')
    paragraph('**问题四：','**问题四：波动电价下的策略扩展。** 将固定分时电价改为附件4给出的实时波动电价，重新计算问题二和问题三，给出两类策略的购电、充放电、紧急购电与实际费用。本文对未来价格的信息假设见第3.1节。')
    paragraph('本问的供需与电价均已给定','本问的供需与电价均已给定，各时段通过储电量递推相互关联。以购电费用最小为目标，联立能量平衡、容量、功率及日周期约束，得到多期调度模型；自由弃电条件下可用等价线性化处理充放电互斥。该物理可行域在后续各问中保持一致。')
    paragraph('本问新增供需预测误差','本问在确定性调度中引入供需预测误差。欠购触发五倍补购，多购仍按计划付费，因此先用历史残差校准日前供需输入，再估计库存用于后续缺口的价值。前者确定应买多少电，后者决定当前应释放多少库存，最终均按实际账单评价。')
    paragraph('本问允许修订未执行合同','本问进一步允许修订尚未执行的购电合同。以当前真实储电量为起点，接入新发布的光伏预报与负载修正，在取消和新增费用约束下重新求解剩余时段。由于下次预报发布后仍可调单，库存价值估计也随合同权限更新。')
    paragraph('本问进一步引入电价不确定性','本问在第二、三问中同时替换计划目标、调单费用和库存价值估计中的电价输入。对尚未发生的时段使用历史价格预测，对已执行时段使用实际价格结算；其余供需预测、风险余量、物理约束和合同规则保持对应关系。重点分析价格波动下的购电与储能行为，以及日内调整的经济效果。')
    paragraph('四问按','四问按照“共同物理约束—供需风险—日内修正—价格不确定性”递进。图1中每一步继承已有调度结构，只扩展可见信息或合同权限；预测为调度提供输入，线性规划生成合同计划，库存价值决定实时放电。')
    md=md.replace('../figures/v6/modeling_overview.png','../figures/v7/modeling_overview.png')
    md=md.replace('具体解释见第3.1节','参数与工作假设见第3.1节')
    md=md.replace('基础模型将题设90%的充放电效率解释为两侧各90%，对应往返效率81%；第5.3节另以往返效率90%检验结果对效率解释的敏感性。','将题设90%的充放电效率解释为两侧各90%，对应往返效率81%；效率对运行费用的影响随第一问结果给出。')
    paragraph('（4）第二至四问','（4）第二至四问的实际储电量连续跨日。日前名义轨迹要求日末储电量不少于6,000 kWh，以保留次日运行所需电量；实际日末状态由实时运行决定，并作为次日初始状态。\n\n（5）第四问将未来交易价格视为决策时未知的量，以历史数据预测；到达交易区间后观测当前价格并按其结算。题面提供全年实际价格，并未明确保证零点已知全天价格，因此本条属于信息可见性的工作假设。计划层采用点预测近似未来价格，历史残差范围描述预测误差，不据此假定未来实际价格必在某一区间内。')
    replace_section('### 3.3','## 四 符号说明','')
    md=md.replace('### 3.2 输入审计与时间边界','### 3.2 数据整理与时间对齐')
    # Replace three defensive subsections with the minimum construction in 5.1
    # and a single results figure in 5.2. The user explicitly removes these headings.
    i=md.index('### 5.3');j=md.index('## 六',i)
    md=md[:i]+r'''无储能时直接按净负载缺口购电，费用为48,052.05元。图4将这一费用与无损储能、实际效率下的最优费用连接：无损储能可降低15,673.52元，效率损失及相应调度变化使费用回升2,748.32元，最终净节省12,925.20元。往返效率从64%提高到100%时，最优费用由38,223.30元降至32,378.53元。

![储能收益分解与往返效率响应](../figures/v7/q1_efficiency.png)

图 4 储能的净收益及效率对最优费用的影响

（a）费用桥接采用相同供需、容量和日初日末状态，逐项展示无损储能收益与实际效率代价；效率代价包含重新优化后的系统费用变化。（b）五组效率均为重新求解所得，主设定往返效率为81%。

'''+md[j:]
    paragraph('完整互斥模型含865个变量',r'''为进行全年多次求解，利用自由弃电处理互斥。记 $\rho=\eta_c\eta_d$，对任一松弛解令 $x_t=\min\{c_t,d_t/\rho\}$，以 $c_t-x_t$、$d_t-\rho x_t$ 和 $w_t+(1-\rho)x_t$ 分别替代充电、放电与弃电。该变换保持储能递推、母线平衡和购电费用，且充放电至少一项为零。因此可先用HiGHS求解线性规划[2]，再逐时消去循环充放电；后续各问复用相同过程。''')
    md=md.replace('### 6.2 同期基线与岭回归预测','### 6.2 供需预测')
    replace_section('### 6.4 实时执行与跨日状态','## 七 第二问的结果与检验',r'''### 6.4 库存价值驱动的实时执行

零点生成计划后，普通购电费已由合同确定，执行阶段的任务是分配储能以减少后续紧急补购。若当前供给富余，先在功率与容量允许范围内充电；若出现缺口，则比较当前放电节费与保留库存的后续价值。

设当前净缺口 $n_t=L_t-V_t-g_t$。对富余供给，充电及弃电量为：

$$
c_t=\min\left\{\max(-n_t,0),M,\frac{10800-S_t}{\eta_c}\right\},\qquad w_t=\max(-n_t-c_t,0).
$$

为估计后续缺口，每次发布取此前至多28个完整日的负载与光伏配对残差，分别叠加至当前预测并截断非负，形成K条净缺口路径。配对保留同一天内负载、光伏误差的联系。对每条路径，以储电量s为状态、紧急补购费为阶段成本，反向递推后续费用：

$$
J_t^{(k)}(s)=\min_{a\in\mathcal A_t^{(k)}(s)}\left\{5p_t e_t^{(k)}(a)+J_{t+1}^{(k)}(s')\right\},\qquad J_{145}^{(k)}(s)=0.
$$

可行集合A包含第一问的储能状态与功率限制，紧急购电只补负载缺口，不能用于充电。路径价值用凸分段线性函数存储[5]，在可行库存区间内计算；其均值近似实际决策的后续费用。历史路径递推用于估值，实时动作始终依据当时可见的供需和库存。

在当前真实缺口 $n_t>0$ 时，选择下一储电状态：

$$
s_{t+1}^{*}\in\arg\min_{z\in[\max\{S_{\min},s_t-\min(n_t,M)/\eta_d\},s_t]}\left\{5p_t[n_t-\eta_d(s_t-z)]+\frac1K\sum_{k=1}^{K}J_{t+1}^{(k)}(z)\right\}.
$$

相应放电量为 $\eta_d(s_t-s_{t+1}^{*})$，剩余缺口由紧急购电补足。求解只需比较可达区间端点与价值函数折点。当后续缺口的单位补购费更高时，模型可以保留部分库存，将电量留给更有价值的时段。实际状态按储能方程逐时更新并连续跨日：

$$
S_{d+1,1}=S_{d,145},\qquad C_2=\sum_{d,t}p_t\left(g_{d,t}^{\mathrm{plan}}+5e_{d,t}\right).
$$

将“出现缺口便尽量放电”的即时平衡规则作为结果分析中的参照，以识别库存配置带来的收益。主策略使用上述价值控制；零终端附加价值由一月验证选定，名义计划仍保持日末6,000 kWh的下限。''')
    md=md.replace('## 七 第二问的结果与检验','## 七 第二问的结果分析')
    md=md.replace('### 7.3 全期购电费用与运行约束','### 7.3 购电费用与储能配置')
    # Drop repeated validation assurances and redundant figure narration.
    for prefix in ['负载同槽七日相关系数','图 7的折线按模型实际替换顺序','图 10展示同一']:
        matches=[p for p in md.split('\n\n') if p.startswith(prefix)]
        for p in matches:md=md.replace(p+'\n\n','')
    md=md.replace('../figures/v6/q3_updates.png','../figures/v7/q3_updates.png')
    paragraph('图 10在同一','图10以点阵标出启用的预报时刻，用同一横轴比较相对仅午夜预报的全年节省；底部连接基础滚动、负载修正选择与库存价值控制的费用。上半图固定50%余量与基础执行器，用于分析信息价值；下半图给出最终方案的形成过程。')
    md=md.replace('第6.5节','第6.4节')
    # Q4 is a direct extension, not a forecasting-method competition.
    replace_section('## 九 第四问','## 十 模型检验与评价',r'''## 九 第四问 波动电价下的购电策略

### 9.1 电价信息与不确定性

第二、三问采用每天重复的分时电价，本问改用附件4的实际价格序列。全年365天共52,560个十分钟电价，范围为0.0076—1.7936元/kWh。图11展示价格的季节变化与日内峰谷结构，同一时刻的交易成本不再逐日相同，储能的跨时段价值也随之变化。

![全年实际电价及月度分布](../figures/v7/annual_price.png)

图 11 波动电价的日内结构与月度分布

（a）全年原始十分钟价格，不截断峰谷。（b）每月全部价格的均值与10%—90%分位范围，表示价格分布而非均值的置信区间。

按第3.1节的信息假设，决策时尚未揭示的价格以历史预测替代。以发布时刻τ到目标时刻j的预测记为 $\hat p_{\tau,j}$，实际价格分解为：

$$
p_j=\hat p_{\tau,j}+\varepsilon_{\tau,j}.
$$

日前分支沿用线性预测结构，以昨日、上周同槽价格，日内与星期正余弦，滞后差值、乘积及两组滞后曲线均值和标准差构成12维特征，标准化后用岭回归估计价格。惩罚系数取1，1月15日、22日及后续每月首日重新拟合，训练窗口最多60日，所有训练目标在拟合前已经发生。预测价格下限为0.001元/kWh。

滚动分支采用昨日与上周同目标时段价格的均值，并在0、6、12、18点按最新可见历史生成预测。两个分支的价格输入规则由一月验证费用确定后固定；此处价格预测作为统一调度模型的输入模块。

价格误差用过去28个完整日、相同发布时刻和目标时段的残差描述。图12展示四个指定日期的午夜预测及残差10%—90%分位范围。区间端点只使用该日之前的数据；全期48,096个午夜预测目标的实际覆盖率为74.36%，因此该范围用来展示预测风险，不作为未来价格的保证边界。

![指定日期的价格预测与历史残差范围](../figures/v7/price_uncertainty.png)

图 12 波动价格的预测偏差与历史残差范围

实线为实际价格，虚线为午夜预测；阴影为点预测加此前28日对应残差的10%—90%分位数，并将下界截至0.001元/kWh。四个日期由题目指定，所有面板使用相同坐标尺度。

### 9.2 日前与滚动模型的价格扩展

第四问继承第二问的供需预测、70%风险余量和固定合同价值控制，以及第三问的滚动光伏预报、50%风险余量、负载修正选择和可调单库存价值。新增内容是：计划目标、合同调整和库存估值中的未来价格均换为当次预测，实际结算使用交易区间的真实价格。

对于日前分支，记 $\mathcal F_d$ 为第二问给定当日名义供需、初始库存及日末下限后的可行域，零点计划满足：

$$
g_d^{0}\in\arg\min_{(g,c,d,w,S)\in\mathcal F_d}\sum_{t=1}^{144}\hat p_{d,0,t}g_t.
$$

对于滚动分支，设Rτ为尚未执行的时段，k、a、b分别为相对午夜合同保留、取消和新增的购电量，满足 $k_t+a_t=g_t^0$、$g_t=k_t+b_t$。在第8.2节的合同规则下，用以下目标替换第三问固定电价目标：

$$
\min\sum_{t\in R_\tau}\hat p_{\tau,t}\left(k_t+0.5a_t+1.5b_t\right).
$$

每次滚动使用当前真实储电量、新预报和当前预测价格，过去的执行结果与午夜原合同保持不变。计划层采用预测价格的确定性等价近似，并以历史验证选择规则；供需误差继续由风险余量处理。本文未将价格误差区间设为硬约束，也未假定点预测能够消除价格风险。

实时执行沿用第6.4节的库存价值决策。当前缺口成本使用已揭示的实际价格p；未来路径费用使用 $\hat p_{\tau,t}$。日前合同锁定时，未来缺口以五倍预测价格估值；滚动分支在下次发布前采用五倍价，之后以1.5倍预测价格近似新增合同机会。该近似只影响库存决策，账单仍按真实合同量和交易价格计算：

$$
C_{4,2}=\sum_{d,t}p_{d,t}\left(g^0_{d,t}+5e_{d,t}\right),\qquad C_{4,3}=\sum_{d,t}p_{d,t}\left(k_{d,t}+0.5a_{d,t}+1.5b_{d,t}+5e_{d,t}\right).
$$

因此，价格变化同时影响“计划在哪个时段买电”“日内是否付费调单”和“当前是否保留库存”。图13给出题目指定日期9月23日的实际运行过程，展示价格、储电量及紧急购电的时间对应关系。

![波动电价与库存控制的实际运行](../figures/v7/price_dispatch.png)

图 13 波动电价下日前与滚动策略的运行过程

（a）实际价格与午夜预测。（b）两个策略的实际储电量。（c）紧急购电量。竖虚线标明6、12、18点的预报更新时间；各子图共享十分钟时间轴，展示题目指定日期的轨迹，不据单日曲线推断全年收益。

### 9.3 实际费用与调度效果

两类策略均在2025年2月1日至12月31日逐时执行，并保持实际储电量跨日连续。表10按保留合同、取消、新增及紧急补购四项归集实际费用；日前策略不能主动调单，后两类合同调整项为零。

表 10 波动电价下两类策略的实际费用构成

| 费用项目 | 日前策略/元 | 滚动策略/元 |
| --- | --- | --- |
| 保留合同购电费 | 13,663,460.94 | 12,764,260.61 |
| 取消合同费用 | 0.00 | 234,774.15 |
| 新增合同购电费 | 0.00 | 765,937.23 |
| 紧急购电费 | 1,102,031.74 | 445,909.02 |
| 总费用 | 14,765,492.68 | 14,210,881.01 |

滚动策略总费用较日前低554,611.67元，降幅为3.76%。其中紧急购电费减少656,122.72元，普通合同及调整相关费用合计增加101,511.05元：日内调整付出额外合同成本，同时降低了高价补购支出。该差额是两套完整策略的运行结果，包含预报、预测输入和调单权限的共同作用。两者期末储电量分别为5,939.92和5,952.82 kWh，按题设账单不计期末残值。

图14进一步检验库存价值控制的贡献。在各自相同价格输入、计划生成规则和初末库存的条件下，相比即时平衡执行，日前节省115,042.37元，滚动节省5,444.97元。后者已能借助调单补足后续缺口，因此进一步优化库存配置的增益较小。

![库存价值控制的月度与全年收益](../figures/v7/inventory_savings.png)

图 14 库存价值控制在波动电价下的收益

（a）两类策略各自相对即时平衡执行的月度节省。（b）全年节省及95%配对区块重采样区间，横轴采用对数坐标以兼顾两类收益的量级差异；采用2,000次、14日区块，日前区间为[59,151.20，186,368.40]元，滚动为[1,953.22，10,500.59]元。区间反映同年固定轨迹的条件波动，不校正模型开发与选择偏差。

表11汇总四问的最终结果，指定日期的购电与储能表保留于附录A。第二问到第三问扩展合同权限，第四问在这两条分支中引入波动价格，均使用相同物理约束与库存决策逻辑。

表 11 四问最终策略与购电费用

| 问题 | 新增信息或权限 | 最终策略 | 购电费用/元 |
| --- | --- | --- | --- |
| 一 | 供需与电价已知 | 确定性线性规划 | 35,126.85 |
| 二 | 供需预测误差 | 风险校准与固定合同库存价值 | 13,991,392.60 |
| 三 | 日内预报、合同调整 | 滚动优化与可调单库存价值 | 13,479,283.32 |
| 四：日前 | 未来电价未知 | 预测价格下的日前策略 | 14,765,492.68 |
| 四：滚动 | 未来电价未知 | 预测价格下的滚动策略 | 14,210,881.01 |

第一问为单日费用，其余为334天费用；各分支分别评价，不合并为同一系统的总费用。''')
    replace_section('## 十 模型检验与评价','## 参考文献',r'''## 十 模型的评价与推广

### 10.1 模型的优点

（1）四问采用统一的能量平衡与储能状态方程，依次扩展供需误差、日内预报和波动价格。各阶段新增变量对应实际的信息或合同变化，便于解释方案的递进关系及不同策略的适用条件。

（2）将计划与执行分开处理。计划层用风险余量权衡多购支出和欠购损失，执行层以库存后续价值确定放电量，使有限电量能够用于更昂贵的后续缺口。第二问在相同初末库存下节省141,219.56元，体现了跨时段配置的作用。

（3）滚动策略保留午夜原合同，以保留、取消和新增量计费；每次仅更新尚未执行的区间，并将真实储电量传递至下一时刻。预测训练、残差估计和模型选择均使用历史信息，策略具有按时间执行的明确流程。

（4）线性规划与分段线性库存价值兼顾求解效率和物理解释。五个最终分支的逐时能量平衡、容量、功率与充放电互斥均满足模型约束，实际费用能够由交易电价和合同账单复算。

### 10.2 模型的不足

（1）未来供需和电价均以有限历史估计，价格在计划层采用点预测近似，尚未联合刻画极端价格与供需缺口的相关风险。历史残差范围的实际覆盖率低于其名义分位宽度，对尖峰时段仍可能低估风险。

（2）库存价值由历史路径均值近似，滚动分支以1.5倍预测价格描述下次调单机会，未完整刻画未来多次预报更新与合同决策。名义日末6,000 kWh下限有助于维持库存，但不等同于最优跨日终端价值。

（3）模型忽略电池老化、区间内快速功率变化和充放电切换成本。现有验证集中于同一年数据，尚需独立年份检验季节变化、极端天气与规则变化下的适应性。

### 10.3 模型的推广

（1）面向含光伏和储能的园区、商业建筑或充电站，可保留共同调度结构，替换负载曲线、设备容量和交易规则。若加入售电或需求响应，应同步修改功率平衡、结算目标及充放电互斥处理，重新检验模型条件。

（2）可用联合场景描述负载、光伏和价格误差，将当前点预测计划扩展为带风险约束的多阶段优化；每次预报发布后更新场景与实际库存，以处理极端缺口和价格尖峰。

（3）可在目标中加入电池吞吐量或循环寿命成本，并用跨日滚动时域估计期末库存价值，从短期购电节费扩展至设备全寿命运行管理。''')
    md=md.replace('## 参考文献','## 十一 参考文献')
    md=re.sub(r'^\[5\] PyTorch[^\n]*\n\n','',md,flags=re.M)
    md=re.sub(r'^\[6\] Cho[^\n]*\n\n','',md,flags=re.M)
    md=md.replace('[7]','[5]')
    # Appendix tables retain the exact supplied-date answers; just renumber after deletions.
    appendix_start=md.index('## 附录 A')
    head,appendix=md[:appendix_start],md[appendix_start:]
    appendix=re.sub(r'表\s*(\d+)',lambda m:'表 '+str(int(m.group(1))-2),appendix)
    md=head+appendix
    replace_section('## 附录 B 支撑文件与完整程序', '\0', '') if False else None
    md=md[:md.index('## 附录 B')]+r'''## 附录 B 支撑文件与计算程序

五份结果工作簿分别为result1.xlsx、result2.xlsx、result3.xlsx、result4-2.xlsx和result4-3.xlsx，均保持原题模板。完整运行数据、代码和图表源文件随支撑材料提供；程序按“数据读取—历史预测—风险校准—计划求解—实时执行—费用结算”的顺序组织。

下面给出价格预测、线性规划及库存价值计算的核心程序。所有实际费用以交易时刻价格结算，原始附件只读。
'''
    # No remaining references to removed subsections or methods.
    assert not re.search(r'GRU|PyTorch|神经网络|第5\.[345]节|第6\.5节|1\.002%',md,re.I)
    assert not re.search(r'^### (3\.3|5\.[345])',md,re.M)
    (ROOT/'reports/完整论文_V7.md').write_text(md,encoding='utf-8')
    equations=re.findall(r'\$\$\s*\n(.*?)\n\$\$',md,re.S)
    (ROOT/'reports/完整论文_V7公式.tex').write_text('\n\n'.join('\\[\n'+x+'\n\\]' for x in equations),encoding='utf-8')
    images=re.findall(r'!\[[^\]]*\]\(([^)]+)\)',md)
    manifest=dict(figures=len(images),tables=len(re.findall(r'^表[^\n]+\n\n(?=\|)',md,re.M)),display_equations=len(equations),
        removed_sections=['3.3','5.3','5.4','5.5','original 9.2-9.5','original 10-11'],
        inherited_results='artifacts/v5b',manuscript='reports/完整论文_V7.md')
    (ROOT/'artifacts/v7/paper_manifest.json').write_text(json.dumps(manifest,ensure_ascii=False,indent=2),encoding='utf-8')
    print(json.dumps(manifest,ensure_ascii=False))


if __name__=='__main__':main()
